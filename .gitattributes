# Shell scripts run inside Linux containers -- CRLF line endings (which a
# Windows clone with core.autocrlf=true would otherwise check out) break the
# shebang line and can cause cryptic syntax errors at container startup.
*.sh text eol=lf
